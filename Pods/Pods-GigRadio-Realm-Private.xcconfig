#include "Pods-GigRadio-Realm.xcconfig"
FRAMEWORK_SEARCH_PATHS = ${PODS_GIGRADIO_REALM_FRAMEWORK_SEARCH_PATHS}
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/Realm" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/CLLocationManager-blocks" "${PODS_ROOT}/Headers/CMDQueryStringSerialization" "${PODS_ROOT}/Headers/ISO8601" "${PODS_ROOT}/Headers/Realm" "${PODS_ROOT}/Headers/Realm+JSON"
OTHER_LDFLAGS = -ObjC ${PODS_GIGRADIO_REALM_OTHER_LDFLAGS}
PODS_ROOT = ${SRCROOT}